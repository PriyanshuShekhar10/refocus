name: "Blog: morning slot (English)"

# One English post per day. Niche rotates across ten community categories
# (med-school … teachers). Afternoon/evening slots are manual-only.
on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: blog-slot-0
  cancel-in-progress: false

jobs:
  generate:
    uses: ./.github/workflows/blog-reusable.yml
    with:
      slot: "0"
      locale: en
    secrets: inherit
